// Placeholder Prisma client stub to keep folder non-empty and compilable
// Replace with: import { PrismaClient } from '@prisma/client'
export class PrismaClient {
  // add minimal shapes to satisfy TypeScript if you wire it later
}
export const prisma = new PrismaClient();
